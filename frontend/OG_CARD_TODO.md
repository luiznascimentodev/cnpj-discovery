# OG card placeholder

`/og-card.png` é referenciado por `index.html` (`og:image` / `twitter:image`).

Asset final ainda **não** foi gerado. Quando o domínio estiver no ar:

1. Gerar PNG **1200×630px** (Figma/Canva ou screenshot Playwright de uma rota `/og-preview`).
2. Salvar como `frontend/public/og-card.png`.
3. Apagar este README.

Até lá, LinkedIn/WhatsApp não mostram thumbnail — o resto do meta-card (título + descrição) ainda renderiza normal.
